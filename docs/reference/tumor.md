# Tumor

The engine that grows a spatially-structured, selection-driven tumor. `GenotypeTumor` is the fast
genotype-level (count-based) default; `GlandularTumor` is the cell-level engine.

::: iscc.tumor.models.count.GenotypeTumor

::: iscc.tumor.models.glandular.GlandularTumor
